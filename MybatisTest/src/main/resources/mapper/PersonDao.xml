<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.transaction.demo.dao.PersonMapper">
    <select id="getByUserName" resultType="string">
        select account_name from person where user_name=#{userName}
    </select>
</mapper>
